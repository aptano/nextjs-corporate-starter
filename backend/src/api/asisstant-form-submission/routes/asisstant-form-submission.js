'use strict';

/**
 * asisstant-form-submission router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::asisstant-form-submission.asisstant-form-submission');
