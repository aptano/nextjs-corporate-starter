'use strict';

/**
 * asisstant-form-submission controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::asisstant-form-submission.asisstant-form-submission');
